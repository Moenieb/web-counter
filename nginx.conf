server {
    listen       80;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
    }

    location /api {
        # bad practice: backend address hardcoded!
		proxy_pass http://webcounter-backend:5000;
	}
}